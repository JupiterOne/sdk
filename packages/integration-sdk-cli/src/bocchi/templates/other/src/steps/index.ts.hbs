{{#each steps}}
import { {{camelCase entity.name}}Steps } from './steps/{{id}}';
{{/each}}

const integrationSteps = [
{{#each steps}}
  ...{{camelCase entity.name}}Steps,
{{/each}}
];

export { integrationSteps };
